# 戰役圖示 v001

原創、可編輯 SVG 圖示，沒有使用第三方圖示包或字型符號。SVG 是來源檔，Godot 直接匯入。色彩使用象牙金線、薄荷龍晶、木色及灰藍石材，讓同一資源在 HUD 與世界投入框保持一致。

`presentation/ui_icons.gd` 集中映射，`campaign_hud.gd` 和 `campaign_view.gd` 共用。數量仍保留數字；主戰役不顯示長文字面板。若修改圖示語意，應同時檢查資源列、需求材料、建築標記與操作按鈕。
