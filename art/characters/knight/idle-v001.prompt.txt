Use case: stylized-concept.
Asset type: production sprite sheet for a Godot 2D pixel-art game, Cyber Kingdom.
Create ONE PNG with a genuinely transparent alpha background, exactly 1024 x 1024 pixels, containing four animation frames in a strict 2-column by 2-row grid. Each cell is exactly 512 x 512. There are no visible cell outlines, captions, labels, cast shadows, ground plane, background color or checkerboard pattern.
Subject: the same original cyber-medieval knight in all four cells, viewed strictly from the side and facing RIGHT. Full-face weathered silver helmet, short burgundy cloak behind the left shoulder, articulated brass mechanical legs, turquoise-lit mechanical forearm, compact medieval plate armor, a straight steel sword held low pointing forward-right. The full body and sword must fit in each cell. Stylized, confident silhouette, premium indie-game pixel art with carefully placed square pixel clusters. Limited palette, bright readable armor edges against actual transparency. Medieval swords-and-magic world with cybernetic prostheses, no guns.
Animation: a subtle four-frame IDLE breathing loop, in reading order top-left, top-right, bottom-left, bottom-right. Feet planted at exactly the same locations relative to each cell in every frame. Frame 1 neutral, frame 2 slight inhale and cape lift, frame 3 peak breath just one pixel higher at the effective pixel scale, frame 4 returning almost to neutral. Same character proportions, armor pieces and sword throughout. No walk cycle and no different poses or directions.
Critical grid geometry: local center of the torso around x=240 in every cell, local sole baseline y=448 in every cell. Helmet top around y=112. Sword can extend to local x=456. Keep generous clear transparent margins, and no pixels across cell boundaries. The second row and column duplicate the same placement translated by precisely 512 pixels.
Pixel technique: draw as if the sheet were a 128 x 128 original scaled exactly 8x with nearest neighbor, so each 512-cell corresponds to a 64 x 64 logical sprite. Crisp deliberate square clusters and flat pixel shading, no antialiasing, no smooth gradients, no blur, no painterly noise. Prefer readable shape over excessive tiny details. Output only the transparent sprite sheet.