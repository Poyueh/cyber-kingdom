# 第二十七課：讓電容少一階
在 FileSystem 選 data/campaign.tres，Inspector 展開 Knight Growth。Capacitor Limit 預設 3；暫時改成 2，儲存後開 scenes/frontier.tscn 按 F6。

這會讓主戰役電容最多兩階，預設容量由 60 降成 40，升級泡泡也只畫兩格階級。受損後仍可充能，但不會因此加到第三階。這種限制存在規則層，並非只把 UI 按鈕藏起來。

試完改回 3。已匯出的桌面程式需重新打包才會套用；本課尚未收到你的操作回報，不記為已掌握。
